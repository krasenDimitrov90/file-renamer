<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Playlist maker</title>
    <link rel="stylesheet" href="/css/songs/check-songs.css">
    <link rel="stylesheet" href="/css/songs/playlist.css">
    <link rel="stylesheet" href="/css/svg.css">
    <link rel="stylesheet" href="/css/go-back-form.css">
    <link rel="icon" type="image/png" href="/favicon.png">
</head>

<body>
    <div class="background_image">
    </div>
    <div class="go-back-form">
        <div class="back-btn-container">
            <button class="back-btn-svg" onclick="history.back()" ></button>
        </div>
    </div>
    <div class="main-container">
        <div class="new-playlist-container">
            <h2>New playlist</h2>
                <% if (newSongs.length === 0) { %>
                    <li>There is no changes yet</li>
                <% } else { %>
                    <div class="album-name">
                        <h3><%= newSongs[0] %></h3>
                    </div>
                <table class="playlist new-playlist">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Track</th>
                        </tr>
                    </thead>
                    <% newSongs.slice(1).forEach((song, i) => {  %>
                        <tr>
                            <td><%= i + 1 %></td>
                            <td><%= song %></td>
                        </tr>
                    <% }) %>
                </table>
                <% } %>
        </div>
        <div class="btns-container">
            <form action="/save-changes" method="POST">
                <input type="hidden"  name="folder" value=<%= folder %> >
                <textarea name="songs" style="display:none;"><%= newSongs.join('\n') %></textarea>
                <button class="submit-btn">SAVE CHANGES</button>
            </form>
            <div >
                <a class="discard-btn" href="/<%= folder %>">DISCARD CHANGES</a>
            </div>
        </div>
    </div>
</body>

</html>